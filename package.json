{
  "name": "aws-waf-ipset-update",
  "version": "1.0.0",
  "description": "A GitHub Action for updating AWS WAF IPSet with current runner IP",
  "main": "src/index.js",
  "scripts": {
    "build": "ncc build src/index.js -o dist && ncc build src/cleanup.js -o dist-cleanup && cp dist-cleanup/index.js dist/cleanup.js && rm -rf dist-cleanup",
    "test": "jest",
    "lint": "eslint src/**/*.js",
    "format": "prettier --write src/**/*.js"
  },
  "keywords": [
    "github-action",
    "aws",
    "waf",
    "ipset",
    "security"
  ],
  "author": "digglife",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/github": "^6.0.0",
    "@aws-sdk/client-wafv2": "^3.450.0",
    "axios": "^1.6.0"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.1",
    "eslint": "^8.54.0",
    "jest": "^29.7.0",
    "js-yaml": "^4.1.0",
    "prettier": "^3.1.0"
  }
}
